# draft-ietf-ippm-stamp-ext-hdr conformance — stamp-suite

Revision frozen: draft-ietf-ippm-stamp-ext-hdr-**11**, 4 July 2026 (verified 2026-07-22)
Source: https://www.ietf.org/archive/id/draft-ietf-ippm-stamp-ext-hdr-11.txt

Summary: 40 clauses — 38 Compliant / 0 Partial / 0 Gap / 0 N-A(implementation) + 2 N-A(optional) / 0 Excluded

> **Chunk F3 (2026-07-23): the remaining MUST cluster is now implemented.**
> The 2 Partial + 7 Gap rows carried since the 2026-07-22 adversarial re-check
> have all moved to Compliant: multi-header request TLVs (3.1-2, 3.2-1),
> real IPv6 extension-header attachment by the sender (3.1-1), the reflector
> capture walk extended to Routing/SRH/Fragment (3.1-6) and to IP-in-IP
> stacked fixed headers (3.2-1, 3.2-5), and MTU-aware sizing + TLV removal on
> both the sender and reflector (3.1-9/10, 3.2-8/9). See each row for the
> file:line evidence and the tests added. The two remaining non-Compliant
> rows (ext-hdr-6-1, ext-hdr-7-1) are N-A optional (MAY / SHOULD-NOT with no
> auto-assignment), unchanged.

> **Adversarial re-check (2026-07-22, historical).** Prior to Chunk F3 all 11
> rows previously marked PROVISIONAL (2 Partial + 9 Gap) had been re-adjudicated
> against the source draft text and the code of the day. Net effect at that
> time: 1 row REFUTED/RECLASSIFIED from Gap to Compliant (ext-hdr-5.3-1, on the
> strength of §5.3 rule 4's explicit C-flag escape valve for "cannot add"); the
> remaining 10 rows were CONFIRMED as genuine gaps/partials. Chunk F3 has since
> closed 9 of those 10 (the 10th, ext-hdr-5.3-1, was already Compliant); no
> row was found to need a role/level correction.

> **Codepoint note.** IANA has not yet allocated TBA1/TBA2/TBA3. This tree uses
> experimental stand-ins: **Type 246** = Reflected IPv6 Extension Header Data
> (TBA1), **Type 247** = Reflected Fixed Header Data (TBA2), **Sub-TLV Type
> 240** = IPv6 Extension Header Control (TBA3), under the `Reflected Test
> Packet Control` TLV (Type 12, draft-ietf-ippm-asymmetrical-pkts-14). All
> three are live-fire values, not registered; interop with any other
> implementation requires agreement on these numbers pending IANA action.
>
> **-11 rework note.** The -11 revision split each of TLV 246/247's Value
> field into a fixed 4-octet **Requested** field (a disambiguation selector,
> left as received) and a **Reflected** field of `Length - 4` octets (the
> matched header's bytes from offset 4 onward). This creates an internal
> tension the draft leaves implicit: §5.1/§5.2 phrase the all-zeros-Requested
> case as "match the **first** length-matching header," which is a
> stateless per-TLV rule, while §3.1 rule 2 / §3.2 rule 2 require multiple
> same-type TLVs to pair **positionally** with the wire-order chain of
> headers (1st TLV ↔ 1st header, 2nd TLV ↔ 2nd header, ...) — a naive
> "first match" applied independently to each TLV would instead pair every
> such TLV with the same first header. This implementation reconciles the
> two with **first-fit-with-consumption**: each matched captured header is
> marked consumed so a later TLV of the same value-length skips it,
> yielding §3.1/§3.2 ordering while still honouring §5.1/§5.2's per-TLV
> first-fit rule. See `src/tlv/list/processing.rs` (`apply_reflected_ext`,
> doc comment above `process_reflected_headers`) and the rows for
> ext-hdr-3.1-6 / ext-hdr-5.1-C below, which carry this note explicitly.

Role legend: Sender / Reflector / Both / N-A.

| ID | Clause (trimmed quote) | Level | Role | Status | Evidence |
|----|------------------------|-------|------|--------|----------|
| ext-hdr-3.1-1 | "the Session-Sender MUST add a corresponding 'Reflected IPv6 Extension Header Data' TLV ... with the length set to the IPv6 extension header length ... to receive a copy" | MUST | Sender | Compliant | **Fixed (Chunk F3).** The sender now attaches real IPv6 extension headers to its own egress packets and emits a matching Type-246 TLV for each. `--attach-ext-hdr KIND[:HEX]` (KIND = `hbh`/`dest`) is parsed by `configuration.rs::parse_attach_ext_hdr_spec` and applied on the egress socket via `sender.rs::apply_attach_ext_hdrs` (`setsockopt(IPPROTO_IPV6, IPV6_HOPOPTS/IPV6_DSTOPTS)`, Linux only — `libc` exports these constants for `linux_like` targets only, not for Darwin), the same sticky-option technique proved on the wire by the netns tier's `tests/netns/mod.rs::set_ipv6_dstopts`. For every attached header, `sender.rs::reflected_header_request_tlvs` pushes a `ReflectedIpv6ExtHdrTlv` request whose Length equals the attached header's on-wire size, with an all-zeros Requested field (byte 0 = Next Header is kernel-assigned and cannot be predicted, so positional pairing per §3.1 rule 2 disambiguates rather than a selector). Platform degradation is honest: on non-Linux or an IPv4 destination the header is not attached (one-time warning) — on IPv4 no Type-246 TLV is emitted, on other platforms the request TLV is still sent. Tests: `sender::tests::attach_ext_hdr_emits_matching_type246_request`, `attach_ext_hdr_custom_hex_sizes_the_request_tlv`; `configuration::tests::attach_ext_hdr_default_and_custom_parse`, `attach_ext_hdr_unknown_kind_is_rejected`, `attach_ext_hdr_non_multiple_of_8_is_rejected`. The live setsockopt send path (like the pre-existing netns 4b) runs only under the privileged netns tier; the TLV-construction half is unit-covered. |
| ext-hdr-3.1-2 | "the corresponding ... TLVs MUST be added, with lengths matching ... and in the same order" (multiple IPv6 ext headers) | MUST | Sender | Compliant | **Fixed (Chunk F3).** `--reflected-ipv6-ext-hdr` is now repeatable (`configuration.rs` field `reflected_ipv6_ext_hdr: Vec<String>`, clap `num_args=0..=1` + `Append`); each occurrence takes `[LEN[:SELECTORHEX]]` and produces one Type-246 TLV of the given Length, in order, via `sender.rs::reflected_header_request_tlvs`. The single-header form and the standalone `--reflected-ipv6-ext-hdr-selector` remain backward-compatible (validated in `configuration.rs::validate_ext_hdr_flags`, which rejects the standalone selector when combined with multiple occurrences or an inline selector). Tests: `sender::tests::ext_hdr_multi_requests_emit_multiple_type246_tlvs_in_order`, `ext_hdr_single_form_backward_compatible_with_standalone_selector`; `configuration::tests::multi_ext_hdr_occurrences_parse_and_validate`, `standalone_selector_rejected_with_multiple_ext_hdr_occurrences`. (Reflector-side handling of *received* multi-TLV requests remains Compliant — see ext-hdr-3.1-6 / ext-hdr-5.1-C.) |
| ext-hdr-3.1-3 | "the Session-Sender MUST NOT add a corresponding ... TLV" (header not required to be reflected) | MUST NOT | Sender | Compliant | Opt-in design: the TLV is added only when `--reflected-ipv6-ext-hdr` is set (`configuration.rs:579`, `sender.rs::reflected_header_request_tlvs`); absent the flag, no Type-246 TLV is ever emitted regardless of what extension headers exist. |
| ext-hdr-3.1-4 | "The number of ... TLVs MUST be less than or equal to the number of IPv6 extension headers" | MUST | Sender | Compliant | Trivial corollary of ext-hdr-3.1-2's single-TLV-emission design: the sender never emits more than one Type-246 TLV, so the invariant holds for any deployment with ≥1 actual extension header. |
| ext-hdr-3.1-5 | Reflector rule 1: "MUST copy the entire IPv6 extension header into the ... TLV" | MUST | Reflector | Compliant | Honoured under the documented -11 split-field reconciliation (see header note): `apply_reflected_ext` + `copy_reflected` (`processing.rs`) copy `header[4..]` into `value[4..]`, leaving `value[..4]` (the Requested selector) exactly as received — matching Figure 6's `Length = full header size` framing. Unit tests `test_reflected_ipv6_ext_hdr_populated_when_captured`, `test_v11_ext_hdr_zero_selector_picks_first_length_match`; on-wire: netns scenario 4b (`scenario_4b_ext_hdr_pnet_capture`, `doc/testing-netns.md` row 4b). |
| ext-hdr-3.1-6 | Reflector rule 2: "each IPv6 extension header MUST be processed in order, starting from the outer header, and copied into the corresponding ... TLV" | MUST | Reflector | Compliant | **Fixed (Chunk F3).** Ordering/pairing was already Compliant via first-fit-with-consumption (`apply_reflected_ext`'s `consumed` bitset; tests `test_v11_ext_hdr_positional_pairing_two_tlvs_two_headers`, `test_v11_ext_hdr_zero_selector_same_length_pairs_in_order`, `test_v11_ext_hdr_zero_selector_first_fit_two_tlvs_reorder`). The chain-walk (`receiver/pnet.rs::extract_ipv6_ext_headers` / `walk_ipv6_ext_header_chain` + `ext_header_len`) now traverses and captures Hop-by-Hop (0), Destination Options (60), **Routing (43, including the Segment Routing Header / routing type 4)** and Fragment (44) — Routing/HBH/DestOpts via the generic `(Hdr Ext Len + 1)*8` container, Fragment as its fixed 8 octets — matching §3.1's own example list ("Routing Header for IPv6 including Segment Routing Header"). The walk deliberately **terminates** at the Authentication Header (51, length in a different unit) and ESP (50, encrypted payload): neither can be reflected, so a Type-246 request targeting them finds no length-matching candidate and correctly takes the §5.1-E C-flag path — documented, not a silent gap. Tests: `receiver::pnet::tests::walk_captures_routing_header_including_srh`, `walk_captures_fragment_header_as_fixed_eight_octets`, `walk_terminates_at_ah_and_esp`, plus the retained `extract_ipv6_ext_headers_stores_records_verbatim_on_wire`. |
| ext-hdr-3.1-7 | "The Session-Sender MUST copy the 'Requested IPv6 Extension Header Data' field ... using the first 4 octets ... if there is an ambiguity" | MUST | Sender | Compliant | `ReflectedIpv6ExtHdrTlv::request_with_selector` (`tlv/typed/reflected_ipv6_ext_hdr.rs:92-96`), wired to CLI via `--reflected-ipv6-ext-hdr-selector` (`configuration.rs:581-591`, consumed in `sender.rs::reflected_header_request_tlvs:1291-1299`). |
| ext-hdr-3.1-8 | "If the Session-Reflector receives ... non-zero values in the 'Requested ...' field ..., the Session-Reflector MUST match the first 4 octets in the corresponding IPv6 extension header ... before copying" | MUST | Reflector | Compliant | `apply_reflected_ext` non-zero-selector branch (`processing.rs:1101-1148`); unit tests `test_reflected_ipv6_ext_hdr_selector_matches_specific_header`, `test_reflected_ipv6_ext_hdr_selector_no_match_sets_c_flag`; end-to-end `tests/tlv_flag_semantics.rs::reflected_ipv6_ext_hdr_selector_matches_specific_header_end_to_end` / `..._no_match_sets_c_flag_end_to_end`. |
| ext-hdr-3.1-9 | "The Session-Sender and Session-Reflector MUST ensure that the resulting test packets do not exceed the IPv6 MTU after adding ... TLVs" | MUST | Both | Compliant | **Fixed (Chunk F3).** Both roles now size against the MTU. **Sender:** before the send loop, `sender.rs::run_sender` computes the egress MTU via `egress_mtu` (`getsockopt(IP_MTU/IPV6_MTU)` on the connected socket, Linux; 1280 IPv6 / 1500 IPv4 fallback) and the worst-case assembled size (IP + attached ext headers + UDP + STAMP base + HMAC/DM/Access TLVs + the header TLVs) and calls `enforce_egress_mtu`. **Reflector:** after `process_reflected_headers_multi`, `apply_semantic_tlv_processing` calls `TlvList::trim_reflected_headers_to_size` keyed to `--reflected-control-max-size` (the operator's egress-MTU stand-in, already used for Type-12 padding); because the reflector fills sender-sized TLVs in place it never grows the reply, so this is the defensive cap the MUST requires. Tests: `sender::tests::enforce_egress_mtu_trims_header_tlvs_to_fit`, `enforce_egress_mtu_keeps_non_header_tlvs`, `enforce_egress_mtu_noop_when_fits`; `tlv::list::processing::tests::test_trim_reflected_headers_to_size_removes_246_before_247`, `test_trim_reflected_headers_to_size_zero_disables`. Mechanism note: the draft leaves the MTU-determination mechanism open; this implements the minimal conforming behaviour (interface/route MTU with a family floor, never active PMTUD probing). |
| ext-hdr-3.1-10 | "If necessary, one or more 'Reflected IPv6 Extension Header Data' TLVs MUST be removed to avoid violating the IPv6 MTU limit" | MUST | Both | Compliant | **Fixed (Chunk F3).** Both the sender (`enforce_egress_mtu`) and reflector (`TlvList::trim_reflected_headers_to_size`) remove one or more Type-246 TLVs when the packet would exceed the MTU, trimming from the tail (Type-246 before Type-247, preserving §3.3 order among survivors) and removing only Types 246/247. Same tests and evidence as ext-hdr-3.1-9. |
| ext-hdr-3.2-1 | "the corresponding 'Reflected Fixed Header Data' TLVs MUST also be added, with lengths matching ... and in the same order" (multiple IP headers) | MUST | Sender | Compliant | **Fixed (Chunk F3).** `--reflected-fixed-hdr` is now repeatable (`configuration.rs` field `reflected_fixed_hdr: Vec<String>`); each occurrence produces one Type-247 TLV (family-sized) in order via `sender.rs::reflected_header_request_tlvs`, all emitted before the Type-246 TLVs per §3.3. The reflector's capture is likewise now multi-header: `CapturedHeaders.fixed_headers: Vec<Vec<u8>>` and `receiver/pnet.rs::descend_ip_tunnel` walk IP-in-IP (protocol 4 / next-header 41) chains outer→inner. Tests: `sender::tests::fixed_hdr_multi_requests_emit_multiple_type247_tlvs`, `fixed_hdr_before_ext_hdr_per_section_3_3`. |
| ext-hdr-3.2-2 | "the Session-Sender MUST NOT add a corresponding 'Reflected Fixed Header Data' TLV" (header not required to be reflected) | MUST NOT | Sender | Compliant | Opt-in via `--reflected-fixed-hdr` (`configuration.rs:571`); absent the flag, no Type-247 TLV is emitted. |
| ext-hdr-3.2-3 | "The number of ... TLVs MUST be less than or equal to the number of IP headers" | MUST | Sender | Compliant | Trivial corollary of the single-TLV/single-fixed-header design (every STAMP/UDP/IP packet has exactly one outer IP header in the supported topology). |
| ext-hdr-3.2-4 | Reflector rule 1: "MUST copy the IP header into the 'Reflected Fixed Header Data' TLV" | MUST | Reflector | Compliant | `apply_reflected_fixed` (`processing.rs:1031-1085`) + `receiver/pnet.rs` fixed-header capture (`handle_packet`, IPv4 lines ~439-450 / IPv6 lines ~470-475). Unit tests `test_reflected_fixed_hdr_populated_when_captured`, `test_v11_fixed_hdr_requested_preserved_reflected_is_offset_4`, `test_reflected_fixed_hdr_ipv6_request_with_ipv6_capture_populated`. No dedicated netns scenario exercises Type 247 specifically (scenario 4a/4b cover Type 246 only) — evidence is unit-level for the capture-to-TLV path. |
| ext-hdr-3.2-5 | Reflector rule 2: "each IP header MUST be processed in order, starting from the outer header, and copied into the corresponding ... TLV" (multiple headers) | MUST | Reflector | Compliant | **Fixed (Chunk F3).** The reflector captures stacked IP fixed headers outer→inner (`receiver/pnet.rs::descend_ip_tunnel`, bounded by `MAX_IP_TUNNEL_DEPTH`) into `CapturedHeaders.fixed_headers` and processes multiple Type-247 TLVs against them with the same first-fit-with-consumption discipline as Type-246 (`processing.rs::apply_reflected_fixed` now takes the record list + a `consumed` set, driven from `apply_reflected_headers`). Successive same-length Type-247 TLVs pair 1st↔outer / 2nd↔inner (§3.2 rule 2), while a non-zero Requested field selects a specific header (§5.2). Tests: `tlv::list::processing::tests::test_multi_fixed_hdr_positional_pairing`, `test_multi_fixed_hdr_selector_picks_specific`, `test_multi_fixed_hdr_mixed_family_lengths`. |
| ext-hdr-3.2-6 | "The Session-Sender MUST copy the 'Requested Fixed Header Data' field ... using the first 4 octets ... if there is an ambiguity" | MUST | Sender | Compliant | `ReflectedFixedHdrTlv::request_with_selector` (`tlv/typed/reflected_fixed_hdr.rs:90-94`), CLI `--reflected-fixed-hdr-selector` (`configuration.rs:593-601`, `sender.rs:1279-1287`). |
| ext-hdr-3.2-7 | "If the Session-Reflector receives ... non-zero values in the 'Requested Fixed Header Data' field ..., it MUST match the first 4 octets in the corresponding IP header before copying" | MUST | Reflector | Compliant | `apply_reflected_fixed` selector branch (`processing.rs:1031-1085`); unit tests `test_reflected_fixed_hdr_selector_match_populates`, `test_reflected_fixed_hdr_selector_mismatch_sets_c_flag`. |
| ext-hdr-3.2-8 | "The Session-Sender and Session-Reflector MUST ensure that the resulting test packets do not exceed the IP MTU after adding 'Reflected Fixed Header Data' TLVs" | MUST | Both | Compliant | **Fixed (Chunk F3).** Same MTU-sizing machinery as ext-hdr-3.1-9, applied to Type-247: `enforce_egress_mtu` (sender) and `TlvList::trim_reflected_headers_to_size` (reflector) both count and, if needed, remove Type-247 TLVs. Same tests and evidence as ext-hdr-3.1-9. |
| ext-hdr-3.2-9 | "one or more 'Reflected Fixed Header Data' TLVs MUST be removed to avoid violating the IP MTU limit" | MUST | Both | Compliant | **Fixed (Chunk F3).** Same removal machinery as ext-hdr-3.1-10, for Type-247 (removed after Type-246 in the tail-first trim order). Same tests and evidence. |
| ext-hdr-3.3-1 | "The 'Reflected Fixed Header Data' TLVs MUST be added before adding the 'Reflected IPv6 Extension Header Data' TLVs" | MUST | Sender | Compliant | `sender.rs::reflected_header_request_tlvs` (`sender.rs:1270-1308`) unconditionally pushes the Type-247 request (if any) before the Type-246 request (if any) — fixed code ordering, not user-configurable, so the sender can never emit them out of order. |
| ext-hdr-3.3-2 | "If ... TLVs are not received in this order, the Session-Reflector MUST return these TLVs with the C flag ... set to 1 ... without copying any data" | MUST | Reflector | Compliant | **Fixed (Chunk G-fix).** `apply_reflected_headers` (`src/tlv/list/processing.rs`) now performs a positional check before the per-TLV dispatch: if any Reflected Fixed Header Data (247) TLV appears after a Reflected IPv6 Extension Header Data (246) TLV (out of §3.3 order), every header TLV is returned with the C flag set (`set_conformant_reflected`) and no data is copied. Applied to both `self.tlvs` and `wire_order_tlvs` via `process_reflected_headers`. Tests: `test_reflected_headers_out_of_order_sets_c_flag_no_copy`, and no-regression `test_reflected_headers_in_order_processed_normally` (`src/tlv/list/processing.rs`). |
| ext-hdr-4-1 | "the Session-Sender MAY not include a corresponding 'Reflected IPv6 Extension Header Data' TLV" (IOAM DEX option type) | MAY | Sender | Compliant | Same opt-in mechanism as ext-hdr-3.1-3 — the sender only ever adds the TLV when explicitly requested, so "not including it" for any use case (including IOAM DEX) is the default, always-available behaviour. |
| ext-hdr-5.1-A | "STAMP test packets MAY carry one or more STAMP TLVs of this type" (Type 246) | MAY | Both | Compliant | `apply_reflected_headers`'s `for tlv in tlvs` loop (`processing.rs:963-1023`) matches and processes every `ReflectedIpv6ExtHdr` TLV present, however many; exercised with two TLVs by `test_v11_ext_hdr_positional_pairing_two_tlvs_two_headers` and `test_reflected_ipv6_ext_hdr_duplicate_nonzero_selector_consumes_successively`. (The shipped *sender* only ever emits 0 or 1 such TLV — ext-hdr-3.1-2 — but the reflector's handling capacity itself is unbounded.) |
| ext-hdr-5.1-B | Requested field "used to disambiguate which IPv6 extension header ... MUST be copied into the Reflected field when multiple ... of the same length are present" | MUST | Reflector | Compliant | Same enforcement point as ext-hdr-3.1-8 (`apply_reflected_ext` non-zero-selector branch); this clause restates the rule at the TLV-definition level. |
| ext-hdr-5.1-C | "When this field is set to all zeros, the Session-Reflector MUST match the first IPv6 extension header in the Session-Sender test packet with the matching length" | MUST | Reflector | Compliant | Implemented as **first-fit-with-consumption** — see the header note above for the full reconciliation rationale (this is the specific clause in tension with §3.1 rule 2 / ext-hdr-3.1-6). Code: `apply_reflected_ext` all-zeros branch (`processing.rs:1101-1148`); doc comment directly above `apply_reflected_ext` (`processing.rs:711-724`) states the reconciliation explicitly. Tests: `test_v11_ext_hdr_zero_selector_picks_first_length_match`, `test_v11_ext_hdr_zero_selector_first_fit_skips_length_mismatch`, `test_v11_ext_hdr_zero_selector_first_fit_two_tlvs_reorder`, `test_v11_ext_hdr_zero_selector_same_length_pairs_in_order`. |
| ext-hdr-5.1-D | "In Session-Sender test packets, this field [Reflected] MUST be initialized to zero" | MUST | Sender | Compliant | `ReflectedIpv6ExtHdrTlv::request_with_capacity` / `request_with_selector` (`tlv/typed/reflected_ipv6_ext_hdr.rs:69-89`) always zero-fill the tail beyond any selector prefix; unit tests `test_request_with_capacity_is_zero_filled`, `test_request_with_selector_prefixes_then_zero_pads`. |
| ext-hdr-5.1-E | "the Session-Reflector MUST return the 'Reflected IPv6 Extension Header Data' TLV with the C flag ... set to 1" when it "could not use it for reflecting any IPv6 extension header received" — "(a) ... mismatch ... (b) ... cannot access ... (c) no ... header matches" | MUST | Reflector | Compliant | `apply_reflected_ext` calls `tlv.set_conformant_reflected()` (`core.rs:619-621`) on every failure branch, matching the draft's own (a)/(b)/(c) enumeration: (a) length mismatch is structural (candidates are pre-filtered by `r.len() == value_len`, so no same-length candidate ⇒ no match), (b) no data-plane access → `log_reflected_hdr_unsupported_once` (`processing.rs:915-926`, exercised by netns scenario 4a on the nix backend), (c) no selector match → `log_reflected_hdr_selector_no_match_once` (`processing.rs:949-959`). |
| ext-hdr-5.2-A | "STAMP test packets MAY carry one or more STAMP TLVs of this type" (Type 247) | MAY | Both | Compliant | `apply_reflected_headers` processes every `ReflectedFixedHdr` TLV present in the loop; not length-restricted to one. In practice at most one candidate fixed header is ever captured (ext-hdr-3.2-1), so multiple Type-247 TLVs in one packet would each independently match the same single candidate rather than being mutually exclusive — acceptable given the single-fixed-header topology this implementation supports. |
| ext-hdr-5.2-B | Requested field "MUST be copied into the Reflected field when multiple IP headers of the same length are present" | MUST | Reflector | Compliant | Same enforcement point as ext-hdr-3.2-7 (`apply_reflected_fixed` selector branch). |
| ext-hdr-5.2-C | "When this field is set to all zeros, the Session-Reflector MUST match the first IP header in the Session-Sender test packet with the matching length" | MUST | Reflector | Compliant | `apply_reflected_fixed` all-zeros branch (`processing.rs:1031-1085`) trivially satisfies "first" with the single captured candidate; genuine multi-candidate first-fit disambiguation for Type 247 is unexercised due to the single-fixed-header capture limitation (cross-reference ext-hdr-3.2-1/3.2-5 Gap). |
| ext-hdr-5.2-D | "In Session-Sender test packets, this field [Reflected] MUST be initialized to zero" | MUST | Sender | Compliant | `ReflectedFixedHdrTlv::request_with_capacity` / `request_for` / `request_with_selector` (`tlv/typed/reflected_fixed_hdr.rs:62-94`) zero-fill the tail; unit tests `test_request_with_capacity_is_zero_filled`, `test_request_with_selector_fills_prefix_and_pads_to_len`. |
| ext-hdr-5.2-E | "the Session-Reflector MUST return the 'Reflected Fixed Header Data' TLV with the C flag ... set to 1" — "(a) ... mismatch ... (b) ... cannot access ... (c) no ... header matches" | MUST | Reflector | Compliant | `apply_reflected_fixed` (`processing.rs:1031-1085`) sets the C flag on all three cases: (a) `log_reflected_hdr_length_mismatch_once`, (b) `log_reflected_hdr_unsupported_once` (netns scenario 4a's nix-backend fallback applies identically to 247, though 4a's assertion targets 246), (c) `log_reflected_hdr_selector_no_match_once`. Unit tests `test_reflected_fixed_hdr_c_flag_when_backend_cant_capture`, `test_reflected_fixed_hdr_length_mismatch_sets_c_flag`, `test_reflected_fixed_hdr_selector_mismatch_sets_c_flag`. |
| ext-hdr-5.3-1 | Sub-TLV rule 1: "The Session-Reflector MUST add new matching IPv6 extension headers in the Session-Reflector STAMP test packet in the same order ... (except the routing extension headers ...)" | MUST | Reflector | Compliant | **Adversarial re-check: REFUTED/RECLASSIFIED from Gap to Compliant, per the audit's own special-care instruction.** Confirmed absent in both backends: no code path in `receiver/nix.rs` or `receiver/pnet.rs` attaches extension headers to an outgoing reply packet — neither via manual serialization nor via `IPV6_HOPOPTS`/`IPV6_DSTOPTS` setsockopt/cmsg (grep confirms zero hits for either on the send path; the existing `src/srv6.rs` SRH-attachment machinery used for RFC 9503 return-path forwarding is architecturally adjacent but attaches a *Routing* Header via `IPV6_RTHDR`, which is exactly the header type §5.3 rule 1 itself *excludes* ("except the routing extension headers..."), so it cannot be reused to satisfy this rule regardless). Quoting rule 4 in full: "If the Session-Reflector cannot add a new matching IPv6 extension header in the Session-Reflector test packet, the Session-Reflector MUST return the 'Reflected Test Packet Control' TLV with the C flag (Conformance) set to 1 in the Sub-TLV Flags of the 'IPv6 Extension Header Control' Sub-TLV ... This can occur, for example, when the Session-Reflector does not support the IPv6 extension header, or when the Session-Reflector cannot access the received IPv6 extension headers from the data plane." Rule 4 is the draft's own explicit, sanctioned escape valve for exactly this situation, and the reflector implements it fully and unconditionally: `set_ipv6_ext_hdr_control_c_flag`/`mark_ipv6_ext_hdr_control_c` (`processing.rs`) is invoked whenever `ipv6_ext_hdr_control_count >= 1` (`receiver/mod.rs:1786-1789`), i.e. on every single occurrence of the sub-TLV, with no conditional path that could produce anything else. Because the reflector's inability to add headers is total and permanent (not input-dependent), for *every possible input* the observed wire behaviour matches one of the two RFC-sanctioned branches of rule 1/rule 4's disjunction (add-and-signal-nothing, or cannot-add-and-signal-C=1) — which is the definition of compliance, even though only the second branch is ever exercised. This is a deliberate, permanent non-goal (documented in `receiver/mod.rs:823-832`'s "Neither backend can do that" comment), not a partial/in-progress implementation, so Compliant is the more accurate label than Partial. |
| ext-hdr-5.3-2 | Sub-TLV rule 2: "In the absence of the ... Sub-TLV ..., the Session-Reflector MAY not add new matching IPv6 extension headers ... local policy" | MAY | Reflector | Compliant | The default (and only) behaviour is to never add reply extension headers, which is exactly the option this MAY describes; `receiver/mod.rs:1786-1789`. |
| ext-hdr-5.3-3 | Sub-TLV rule 3: "The IPv6 extension headers received ... MUST be copied and reflected in the corresponding ... TLVs ... regardless of whether ... Sub-TLV is present or not" | MUST | Reflector | Compliant | Type-246 processing (`tlvs.process_reflected_headers_multi`, `receiver/mod.rs:1718`) runs unconditionally and independently of the Reflected Control TLV / sub-TLV handling (`receiver/mod.rs:1733-1789`); confirmed by inline comment "246 reflection is unaffected (handled independently, rule 3)" at `receiver/mod.rs:1784-1785`. |
| ext-hdr-5.3-4 | Sub-TLV rule 4: "If the Session-Reflector cannot add a new matching IPv6 extension header ..., the Session-Reflector MUST return the 'Reflected Test Packet Control' TLV with the C flag ... set to 1 in the Sub-TLV Flags" | MUST | Reflector | Compliant | `set_ipv6_ext_hdr_control_c_flag` / `mark_ipv6_ext_hdr_control_c` (`processing.rs`), invoked whenever `ipv6_ext_hdr_control_count >= 1` (`receiver/mod.rs:1786-1789`) — always true here since ext-hdr-5.3-1 is a confirmed Gap. Test: `tests/tlv_flag_semantics.rs::reflected_control_single_ext_hdr_control_sets_c_on_subtlv`. |
| ext-hdr-5.3-5 | "STAMP test packets MUST NOT carry more than one 'IPv6 Extension Header Control' Sub-TLV in a 'Reflected Test Packet Control' TLV" | MUST NOT | Sender | Compliant | `sender.rs::build_reflected_control_tlv` (`sender.rs:1229-1255`) appends the sub-TLV bytes at most once per constructed TLV; the shipped sender has no mechanism to emit a duplicate. |
| ext-hdr-5.3-6 | "If the ... TLV ... contains more than one ... Sub-TLV, the Session-Reflector MUST return the ... TLV with the C flag ... set ... in the Sub-TLV Flags of all ... Sub-TLVs" | MUST | Reflector | Compliant | `mark_ipv6_ext_hdr_control_c` (`processing.rs:1189-1218`) walks every sub-TLV in the value and sets the C flag on **every** occurrence of type 240, not just the first. Test: `tests/tlv_flag_semantics.rs::reflected_control_duplicate_ext_hdr_control_sets_c_on_all_copies` (asserts C set on both the 1st and 2nd offending copy). |
| ext-hdr-6-1 | "An operator MAY provision a local policy on a Session-Reflector to not copy and reflect the received IPv6 extension headers and IP headers ... to avoid exposing ... network information" | MAY | Reflector | N-A | Optional (MAY) operator-policy feature; no such opt-out flag exists in `configuration.rs`/`receiver/mod.rs` today. Non-implementation of a MAY is not a conformance gap, but is noted here as a currently-unavailable operational control. |
| ext-hdr-7-1 | "implementations SHOULD NOT assign STAMP Session-IDs ... in a predictable manner" | SHOULD NOT | Sender | N-A | STAMP SSIDs are entirely operator-supplied via `--ssid` (`configuration.rs:259`, defaulting to `unwrap_or(0)` when unset, e.g. `sender.rs:526,542`); the implementation performs no automatic SSID assignment for this SHOULD NOT to apply to. Caveat: an operator who never sets `--ssid` gets the maximally predictable value 0 for every session — an operational risk, not a code-level violation of an auto-assignment rule that doesn't exist here. |

## Report notes

* Sections 2.1 (keyword boilerplate), 3.1.1 (One-Way/Two-Way Measurement Types — narrative, contains zero independent RFC 2119 keywords; its behavioural requirement is stated normatively only in §5.3, rows ext-hdr-5.3-1/5.3-2), 3.3's descriptive lead-in, §8 (Implementation Status, editorial), and §9 (IANA Considerations, non-implementation) contributed no additional MUST/SHOULD/MAY clauses beyond what is tabled above and are excluded from the row count.
* **Adversarial re-check completed (2026-07-22).** The 2 Partial rows (ext-hdr-3.1-1, ext-hdr-3.1-6) and 9 Gap rows previously marked PROVISIONAL have all been re-adjudicated. ext-hdr-5.3-1 is REFUTED/RECLASSIFIED from Gap to Compliant (§5.3 rule 4's C-flag escape valve for "cannot add" fully covers this reflector's permanent, documented inability — see its row for the full reasoning). The other 10 rows are CONFIRMED as genuine findings; none required a role/level correction (the IPv6/IP-MTU rows in particular were double-checked against the "MUST vs SHOULD, sender vs reflector" question raised by the audit brief and found to already be correctly graded as MUST/Both).
* **Chunk F3 completed (2026-07-23).** The 9 remaining non-Compliant MUST rows adjudicated above were implemented and moved to Compliant: multi-header request TLVs (3.1-2, 3.2-1), real IPv6 extension-header attachment (3.1-1), the Routing/SRH/Fragment capture walk (3.1-6), IP-in-IP multi-fixed-header capture + processing (3.2-1, 3.2-5), and MTU-aware sizing + TLV removal on both roles (3.1-9/10, 3.2-8/9). Only ext-hdr-6-1 and ext-hdr-7-1 remain non-Compliant, both N-A optional (a MAY operator opt-out and a SHOULD-NOT with no auto-assignment). Where the draft leaves a mechanism open (MTU determination), the minimal conforming behaviour was chosen and documented in-row (route/interface MTU with a family floor; the reflector reuses `--reflected-control-max-size` as its egress-MTU stand-in). Platform degradation is explicit: header attachment and route-MTU lookup are Linux/macOS (Linux for `IP_MTU`); other platforms warn and fall back without failing.
