# draft-ietf-ippm-asymmetrical-pkts-14 conformance — stamp-suite
Revision frozen: [draft-ietf-ippm-asymmetrical-pkts-14](https://datatracker.ietf.org/doc/html/draft-ietf-ippm-asymmetrical-pkts-14), 16 March 2026; §5 evidence updated 9 September 2026.
Summary: 47 clauses — 38 Compliant / 1 Partial / 0 Gap / 7 N-A / 1 Excluded

Type-12 ordering responses and Linux route-MTU enforcement are implemented.
Timing remains best-effort, and exact sizing has the limits in asym-3-08.
This is an Internet-Draft profile.

| ID | Clause (trimmed quote) | Level | Role | Status | Evidence |
|----|------------------------|-------|------|--------|----------|
| asym-3-01 | "Length is a two-octet field. The value is variable, and MUST NOT be smaller than 12 octets." | MUST NOT | Both | Compliant | `src/tlv/core.rs::REFLECTED_CONTROL_TLV_MIN_VALUE_SIZE` (`REFLECTED_CONTROL_TLV_MIN_VALUE_SIZE = 12`); `src/tlv/typed/reflected_control.rs:86-89` (`decode_value` rejects `value.len() < 12` with `TlvError::InvalidReflectedControlLength`); encoder (`encode_value:102-118`) always pads to 12+. Tests: `reflected_control.rs::test_reflected_control_wire_format_pads_to_min_12_bytes`, `test_reflected_control_invalid_length_rejected` (0..12 all rejected); `tests/tlv_flag_semantics.rs::a1_reflected_control_min_length_12_pre_14_rejected` (an 8-byte value round-trips through the reflector's lenient parser and comes back with the M-flag set). |
| asym-3-02 | "A Session-Sender MUST zero this flag [C] on transmission" | MUST | Sender | Compliant | `src/tlv/core.rs::TlvFlags::for_sender` (`TlvFlags::for_sender()` leaves `conformant_reflected: false`, only `unrecognized: true`); `src/tlv/traits.rs:40-44` (`to_raw()` → `RawTlv::new()` → `for_sender()`); `src/sender.rs::build_reflected_control_tlv` (`build_reflected_control_tlv`) never calls a C-flag setter. No code path on the sender side ever sets this bit. |
| asym-3-03 | "the Session-Reflector MUST ignore its value on the receipt of a STAMP test packet with a STAMP TLV" [C flag] | MUST | Reflector | Compliant | `src/receiver/mod.rs::apply_semantic_tlv_processing` (`apply_semantic_tlv_processing`'s Type-12 branch) never reads `tlv.flags.conformant_reflected` on the incoming TLV — only `get_reflected_control_request()` (`src/tlv/list/processing.rs::get_reflected_control_request`, parses fixed fields + sub-TLVs, not flags) feeds the decision. Test: `tests/tlv_flag_semantics.rs::incoming_c_flag_on_request_is_ignored` — sender sets C=1 on the request; reflector's echoed C is derived independently (0, since the request is conformant). |
| asym-3-04 | "A Session-Sender MAY include the Reflected Test Packet Control TLV in a STAMP test packet." | MAY | Sender | Compliant | `src/configuration.rs::Configuration::reflected_control_count` (`--reflected-control-count/-length/-interval-ns/-no-ext-hdr`); `src/sender.rs::run_sender_with_output` (`build_reflected_control_tlv`, wired into the outgoing TLV chain when `count > 1` or `no_ext_hdr`). Test: `sender.rs::build_reflected_control_tlv_only_when_requested`. |
| asym-3-05 | "the Session-Reflector MUST transmit a sequence of reflected test packets according to the following rules" | MUST | Reflector | Compliant | `apply_semantic_tlv_processing` (`src/receiver/mod.rs`) computes `ReflectedControlBehavior`; both backends enqueue a `Transmission` and use `ReplyQueue` (`src/receiver/transmit.rs`) for additional sends. `tests/burst_transmission_test.rs` verifies per-copy fields on the wire. See asym-3-11 for the timing qualification and the remediation tracker for remaining Type-12 findings. |
| asym-3-06 | "The length of the reflected test packet MUST be the largest of [(a) base packet incl. TLVs excl. Extra Padding, (b) requested length aligned to a four-octet boundary]" | MUST | Reflector | Compliant | `receiver/mod.rs::apply_semantic_tlv_processing` — strips Extra Padding (`tlvs.remove_extra_padding_tlvs()`), computes `current = base_bytes.len() + tlvs.wire_size()`, `aligned_req = length.div_ceil(4)*4`, and pads to `target = aligned_req.min(cap)` only when `target > current`. Tests: `tlv_flag_semantics.rs::reflected_control_strips_echoed_extra_padding` (rule (a): can shrink below the received size), `::reflected_control_pads_to_four_octet_aligned_length` (rule (b): 101→104), `::a1_reflected_control_length_padding_within_cap`. |
| asym-3-07 | "the Session-Reflector MUST use the Extra Padding TLV ... to increase the length of the reflected test packet" [when rule (a)/(b) length exceeds the RFC 8972 §4 base length] | MUST | Reflector | Compliant | `receiver/mod.rs::apply_semantic_tlv_processing` — when `target > current`, pushes `ExtraPaddingTlv::new_zeros(delta - TLV_HEADER_SIZE)`. Test: `tlv_flag_semantics.rs::a1_reflected_control_length_padding_within_cap` (asserts an `ExtraPadding` TLV is present); netns `scenario_6_type12_multi_reply` (payload > base 44 octets on the wire). |
| asym-3-08 | "If the calculated length of the reflected packet exceeds the [egress] MTU ..., the Session-Reflector MUST set the C flag ... to 1, and MUST transmit a single reflected packet of the length equal to MTU of the egress interface" | MUST | Reflector | Partial | Linux send-time route MTU limits apply to both backends, each burst copy, and fallbacks (`src/receiver/transmit.rs`). Unknown budgets drop the reply; padding shrinks without truncating mandatory fields. A 1–3-byte remainder stays short because no TLV header fits. Non-Linux route enforcement is absent. Tests: `tests/route_mtu_test.rs` and [namespace MTU regression](../testing-netns.md#reply-route-mtu-regression). |
| asym-3-09 | "Otherwise, the Session-Reflector MUST set the C flag to 0 in each reflected test packet" [MTU-exceeded context] | MUST | Reflector | Compliant | `receiver/mod.rs::apply_semantic_tlv_processing` — `set_reflected_control_c_flag()` is only called in the `non_conformant` branch; the conformant branch (`receiver/mod.rs::apply_semantic_tlv_processing`) leaves C untouched (default 0). Test: `tlv_flag_semantics.rs::a1_reflected_control_length_padding_within_cap` asserts `flags & 0x10 == 0x00`; `::c_flag_clear_when_reflected_control_request_within_caps`. |
| asym-3-10 | "The number of reflected test packets in the sequence MUST equal the value of the Number of the Reflected Packets field." | MUST | Reflector | Compliant | `ReflectedControlBehavior` supplies the admitted extra-copy count. `Transmission::send_next` and `ReplyQueue::schedule_next` (`src/receiver/transmit.rs`) send the initial reply plus those copies, with per-copy rate limiting and stop-on-failure semantics. Both backends use this path. `tests/burst_transmission_test.rs` observes exactly three burst replies around an interleaved ordinary reply; the pnet worker regression also observes all copies. Resource/transport failure can shorten a burst. |
| asym-3-11 | "the interval between the transmission of two consecutive reflected packets in the sequence MUST be equal to the value in the Interval Between the Reflected Packets field in nanoseconds" | MUST | Reflector | Compliant | The shared semantic path rejects requests below `reflected_control_min_interval_ns`. `ReplyQueue::schedule_next` (`src/receiver/transmit.rs`) schedules the next copy at the current time plus the admitted interval after the preceding successful send. Both backends use deadline scheduling; pnet capture does not sleep between copies. **Timing qualification:** OS scheduling and finalization overhead can lengthen the on-wire gap. Exact nanosecond equality is not established by this implementation or the loopback tests; the recorded Compliant score is qualified by this timing limit. |
| asym-3-12 | "a Session-Reflector that supports the Reflected Test Control TLV MUST enforce limits on both the data rate (bytes per second) and the total data volume (bytes) of the STAMP payload it generates in response to an incoming test packet" | MUST | Reflector | Compliant | `--reflected-control-max-count`, `--reflected-control-min-interval-ns`, and `--reflected-control-max-size` are enforced together in `apply_semantic_tlv_processing` (`src/receiver/mod.rs`). `Transmission::send_next` (`src/receiver/transmit.rs`) also rate-limits each extra copy by source IP in both backends. `ReplyBudget` bounds queued bursts through `--reflector-queue-capacity`; see `tests/reply_queue_test.rs`. |
| asym-3-13 | "If a test packet is received that would generate traffic that exceeds either of these limits, the Session-Reflector MUST set the C flag to 1, and MUST transmit a single reflected packet of the length calculated by the rules listed above." | MUST | Reflector | Compliant | `receiver/mod.rs::apply_semantic_tlv_processing` — `requested_count > max_count` OR `interval < min_interval` sets `non_conformant = true`, which forces `extra_copies = 0` (single reply) and `set_reflected_control_c_flag()`. Tests: `tlv_flag_semantics.rs::reflected_control_count_above_cap_collapses_to_single_reply`, `::reflected_control_interval_below_floor_collapses_to_single_reply` (both assert `extra == 0` and, for the latter, C=1 on the wire). |
| asym-3-14 | "Otherwise, the Session-Reflector MUST set the C flag to 0 in each reflected test packet." [rate/volume context] | MUST | Reflector | Compliant | Same code path as asym-3-09 (`receiver/mod.rs::apply_semantic_tlv_processing` — the single `non_conformant` flag covers both the MTU and the rate/volume triggers, and C is only set when it is true). Test: `tlv_flag_semantics.rs::c_flag_clear_when_reflected_control_request_within_caps`. |
| asym-3-15 | "If the Number of Reflected Packets field is set to zero, the Session-Reflector MUST NOT send any reflected packets." | MUST NOT | Reflector | Compliant | `reflected_control_max_count = 0` disables requested burst/size behavior and returns one C-flagged unamplified reply. With control enabled, a new request with count zero suppresses reflection. A non-new request instead gets the §5 U-flag response. See `apply_semantic_tlv_processing` (`src/receiver/mod.rs`) and replay/control tests. |
| asym-3-16 | "Furthermore, in this case, the Session-Reflector SHOULD discard the received STAMP test packet." | SHOULD | Reflector | Compliant | Enabled Type-12 processing suppresses a new request with count zero. Disabled control and non-new requests follow the exceptions in asym-3-15 (`src/receiver/mod.rs::apply_semantic_tlv_processing`). |
| asym-3-17 | "a local policy MAY override this default behavior and specify an alternative handling" | MAY | Reflector | Compliant | Local control policy may reject requested values. Disabling Type 12 returns one C-flagged response; non-new requests use the single U-flagged §5 response. See asym-3-15 and `src/receiver/mod.rs::apply_semantic_tlv_processing`. |
| asym-3.1-01 | "A multicast network that uses an active performance measurement method for In-Service rate estimation MUST include a rate control mechanism that bounds and regulates the generation of measurement packets." | MUST | N-A | N-A | This is a requirement on the *multicast network's* deployment/design (the distribution tree, replication behaviour), not on a single Session-Sender or Session-Reflector process. stamp-suite implements the STAMP endpoints, not the multicast fabric; no code artifact can satisfy or violate this network-level clause. |
| asym-3.1-02 | "The rate control mechanism MUST ensure that probe traffic remains non-intrusive, predictable, and consistent with the operational characteristics of the multicast topology." | MUST | N-A | N-A | Same rationale as asym-3.1-01 — a network-design requirement, not an endpoint-software requirement. |
| asym-3.1-03 | "implementations SHOULD provide operators with the ability to configure rate limits and pacing parameters that prevent excessive or uneven probe replication" | SHOULD | Reflector | Compliant | `src/configuration.rs::Configuration::reflected_control_max_size` — `--reflected-control-max-count`, `--reflected-control-max-size`, `--reflected-control-min-interval-ns` are all operator-configurable at runtime (also live-mutable via the control-plane per `doc/control-plane.md`'s `RuntimeCaps` atomics, `receiver/mod.rs::RuntimeCaps`). |
| asym-3.1.1-01 | "lengths of MAC Address Group Mask and MAC Address Group fields MUST be equal, valid values for the Sub-TLV Length are 4, 12, and 16" | MUST | Both | Compliant | Reflector validation accepts L2 Address Group value lengths 4/12/16 and requires equal address halves (`src/tlv/typed/reflected_control.rs`, `src/receiver/mod.rs`). The optional sender L2 sub-TLV has no typed CLI emission path. Tests cover accepted lengths and mismatched groups. |
| asym-3.1.1-02 | "Any other value MUST be considered by the Session-Reflector as a malformed sub-TLV." | MUST | Reflector | Compliant | Invalid L2 Address Group lengths are skipped by address-group parsing. The draft does not prescribe a specific malformed response for this case. Equal-length supported groups receive the checks in asym-3.1.1-01. |
| asym-3.1.1-03 | "If the Session-Reflector applies the value of the Layer 2 Address Group Mask field ... and the result is equal to the value of the Layer 2 Address Group field, then the Session-Reflector MUST stop processing the Layer 2 Address Group sub-TLV and continue processing the received test packet." | MUST | Reflector | Compliant | `receiver/mod.rs::l2_group_matches_any_local` (`l2_group_matches_any_local`, bitwise AND against every local MAC); `receiver/mod.rs::apply_semantic_tlv_processing` (a match leaves `l2_matches == Some(true)`, which does not trigger the drop). Tests: `receiver/mod.rs::l2_group_matches_any_local_exact_match`, `_masked_match`, `_any_of_multiple_locals`; `tests/tlv_flag_semantics.rs::a1_reflected_control_l2_match_replies_normally`; `tests/netns_conformance.rs::scenario_5_address_group_filters` (L2-match case, real MAC on the wire). |
| asym-3.1.1-04 | "If no matches are found, the Session-Reflector MUST stop processing the received packet." [L2] | MUST | Reflector | Compliant | `receiver/mod.rs::apply_semantic_tlv_processing` — `l2_matches == Some(false)` → `return None` (full drop, no reply). Tests: `tests/tlv_flag_semantics.rs::a1_reflected_control_l2_mismatch_suppresses_reply`; `tests/netns_conformance.rs::scenario_5_address_group_filters` (L2-miss: asserts no reply captured). |
| asym-3.1.2-01 | "Sub-TLV Length is a two-octet field whose value equals either 8 ... or 20 ... Any other value MUST be considered by the Session-Reflector as a malformed sub-TLV." | MUST | Reflector | Compliant | The reflector accepts L3 Address Group value lengths 8/20 and skips invalid lengths (`src/tlv/typed/reflected_control.rs`). The invalid-length branch is supported by code inspection; no dedicated bad-L3 wire test is cited. |
| asym-3.1.2-02 | "Reserved: A three-octet field. The field MUST be set to zeros on transmission" | MUST | Sender | N-A | The optional sender L3 Address Group is not originated. Reflector parsing ignores its reserved fields (`src/tlv/typed/reflected_control.rs`). |
| asym-3.1.2-03 | "if the Session-Reflector applies it ... and the result is equal to the value in the IP Prefix field, then the Session-Reflector MUST stop processing the Layer 3 Address Group sub-TLV and continue processing the received test packet." | MUST | Reflector | Compliant | `receiver/mod.rs::l3_group_matches_any_local` (`l3_group_matches_any_local`: builds the prefix mask from `prefix_len`, ANDs against each local address of matching family/length); `receiver/mod.rs::apply_semantic_tlv_processing` (match leaves `l3_matches == Some(true)`, no drop). Tests: `tests/tlv_flag_semantics.rs::a1_reflected_control_l2_and_l3_both_match_replies_normally`; `tests/netns_conformance.rs::scenario_5_address_group_filters` (L3-match case, real IPv4 /32 on the wire). |
| asym-3.1.2-04 | "If no matches are found, the Session-Reflector MUST stop processing the received packet." [L3] | MUST | Reflector | Compliant | `receiver/mod.rs::apply_semantic_tlv_processing` — `l3_matches == Some(false)` → `return None`. Tests: `tests/tlv_flag_semantics.rs::a1_reflected_control_l3_mismatch_suppresses_reply`; `tests/netns_conformance.rs::scenario_5_address_group_filters` (L3-miss: asserts no reply captured). |
| asym-4.1.1-01 | "A service subscriber performing extensive rate measurements on the operational network, SHOULD consider the Consideration 6 ... and be mindful of limits placed on their service by the Service Provider." | SHOULD | N-A | N-A | Guidance addressed to a human operator/subscriber's judgment about their own SLA, not an implementable software behaviour; the referenced [I-D.ietf-ippm-capacity-protocol] Consideration 6 is itself operational advice with no protocol action to encode. |
| asym-4.2-01 | "The Session-Reflector MUST use the source IP address of the received STAMP test packet as the destination IP address of the reflected test packet" | MUST | Reflector | Compliant | `Transmission::send_next` (`src/receiver/transmit.rs`) defaults the target to the captured request source supplied by either backend. An authorized Return Path alternate can override it. Live reply delivery is exercised in `tests/burst_transmission_test.rs` and existing loopback tests. |
| asym-4.2-02 | "and MUST use one of the IP addresses associated with the node as the source IP address for that packet" | MUST | Reflector | Compliant | Both backends reply from the UDP socket bound at startup to a local interface address (standard OS-enforced source-address selection for a bound socket) — `nix.rs` binds the reflector socket to `--local-addr`/wildcard at startup; `pnet.rs` likewise. No code path can emit a reply with a foreign source address. E2e: same round-trip evidence as asym-4.2-01. |
| asym-4.3-01 | "a Session-Sender MUST NOT include a Return Path Control Code Sub-TLV with the Control Code flag set to No Reply Requested in the same test packet as the Reflected Test Packet Control TLV is non-zero." | MUST NOT | Sender | Compliant | `src/configuration.rs::Configuration::validate` — `validate()` rejects `return_path_cc == Some(0)` combined with `reflected_control_count > 1 \|\| reflected_control_no_ext_hdr` at CLI-parse time, quoting this exact draft section in the error message. Test: `configuration.rs::test_return_path_no_reply_conflicts_with_reflected_control`. |
| asym-4.3-02 | "A Session-Reflector that supports both TLVs MUST set the U flag to 1 in Return Path and Reflected Test Packet Control TLVs in the reflected STAMP packet." | MUST | Reflector | Compliant | `receiver/mod.rs::apply_semantic_tlv_processing` — on detecting the conflict (`return_path_action == SuppressReply && req.number_of_reflected_packets != 0`), calls `tlvs.set_reflected_control_u_flag()` (`src/tlv/list/processing.rs::set_reflected_control_u_flag`) and `tlvs.set_return_path_u_flag()`, then forces `return_path_action = Normal` (so a reply IS sent). Test: `tests/tlv_flag_semantics.rs::return_path_no_reply_conflict_sets_u_on_both_tlvs` (asserts U=1 on both echoed TLVs and that a reply is sent). |
| asym-4.3-03 | "the Session-Reflector SHOULD log a notification to inform an operator about the misconstructed STAMP packet." | SHOULD | Reflector | Compliant | `receiver/mod.rs::apply_semantic_tlv_processing` — `log::warn!("STAMP packet combines Return Path 'no reply requested' with a non-zero Reflected Test Packet Control TLV; setting U on both ...")`, unconditionally executed on the same code path as asym-4.3-02. |
| asym-5-01 | "spoofed STAMP test packets with the Reflected Test Packet Control TLV can be exploited to conduct a Denial-of-Service (DoS) attack. Hence, implementations MUST use an identity protection mechanism." | MUST | Both | Compliant | stamp-suite provides the STAMP Authenticated mode (RFC 8762 §4.4, `stamp_modes.rs`, `packets.rs` auth packet structs) and the HMAC TLV (RFC 8972 §4.4, `--hmac-key`/`--hmac-key-file`/`--hmac-key-dir`, `src/configuration.rs::Configuration::hmac_key`, `crypto::HmacKeySet`) as the available identity-protection mechanisms, plus a per-source `RateLimiter` (`receiver/mod.rs::RateLimiter`) as a spoofing-resilient volume backstop. The draft's own example ("verify ... against a pre-defined list of trusted nodes") is illustrative, not mandatory — HMAC/auth-mode is the standard STAMP identity mechanism this satisfies. Enabling it for a given session is an operator choice (not auto-forced when Type 12 is used specifically); see asym-5-03. |
| asym-5-02 | "an implementation that supports this specification MUST provide administrative control of support of the Reflected Test Packet Control TLV on a Session-Reflector with it being disabled by default." | MUST | Reflector | Compliant | `src/configuration.rs::Configuration::reflected_control_max_count` (`--reflected-control-max-count`, `default_value_t = 0`, doc comment: "Default 0, which **disables** asymmetric reflection ... This honours draft-ietf-ippm-asymmetrical-pkts-14 §5"); `receiver/mod.rs::apply_semantic_tlv_processing` (`max_count == 0` branch: never amplifies, always echoes C=1). Tests: `configuration.rs::test_reflected_control_max_count_defaults_to_zero`; `tests/tlv_flag_semantics.rs::reflected_control_disabled_by_default_emits_no_extra_copies`. Also runtime-adjustable via the control plane (`RuntimeCaps`, `receiver/mod.rs::RuntimeCaps`) without a restart. |
| asym-5-03 | "either STAMP authentication mode [RFC8762] or HMAC TLV [RFC8972] SHOULD be used for a STAMP test session containing the Reflected Test Packet Control TLV." | SHOULD | Both | Compliant | Capability confirmed present and combinable: `apply_semantic_tlv_processing` (`receiver/mod.rs::apply_semantic_tlv_processing`) processes the Reflected Control TLV and computes/attaches the response HMAC in the same function (HMAC set last, `receiver/mod.rs::apply_semantic_tlv_processing`), and is invoked identically from both `assemble_auth_answer_with_tlvs` and `assemble_unauth_answer_with_tlvs` (`receiver/mod.rs::assemble_unauth_answer_with_tlvs`). `configuration.rs` places no mutual-exclusion between `--hmac-key`/auth mode and `--reflected-control-*`. As a SHOULD (operator-facing recommendation on *usage*, not a mandated code behaviour), the implementation's job is to not preclude it — confirmed. |
| asym-5-04 | "a Session-Reflector implementation that supports the new TLV MUST provide a mechanism to limit the reflection rate and volume of STAMP test packets" | MUST | Reflector | Compliant | Restatement of asym-3-12; same evidence (`--reflected-control-max-count/-max-size/-min-interval-ns`, `receiver/mod.rs::apply_semantic_tlv_processing`, plus the per-source `RateLimiter`). |
| asym-5-05 | "parameters in the first STAMP test packet with the Reflected Test Packet Control TLV MUST be selected conservatively." | MUST | Sender | Compliant | `src/configuration.rs::Configuration::reflected_control_count` — sender defaults are maximally conservative: `reflected_control_count` defaults to `1` (TLV omitted entirely unless the operator explicitly opts in, per `src/sender.rs::build_reflected_control_tlv`'s `count <= 1 && !no_ext_hdr` early return), `reflected_control_length` defaults to `0` (no padding requested), `reflected_control_interval_ns` defaults to `1_000_000` (1 ms, well above the reflector's 1 µs floor). An operator must deliberately raise `--reflected-control-count` above 1 to activate any amplification at all. |
| asym-5-06 | "a Session-Sender SHOULD sign packets using the HMAC TLV when sending such messages in unauthenticated mode [RFC8762]." | SHOULD | Sender | Compliant | Capability confirmed: `--hmac-key`/`--hmac-key-file` (`src/configuration.rs::Configuration::hmac_key`) are independent of `--is-auth`/`--reflected-control-*` (no `conflicts_with` between them; `validate()` at `src/configuration.rs::Configuration::validate` imposes no such restriction), so an unauthenticated-mode sender can attach the RFC 8972 §4.4 HMAC TLV alongside a Type 12 request. Operator-driven, as befits a SHOULD. |
| asym-5-07 | "a STAMP Session-Reflector SHOULD use the value of the Sequence Number field [RFC8762] of the received STAMP test packet [to detect non-monotonic/replayed traffic]" | SHOULD | Reflector | Compliant | `evaluate_replay` and `process_stamp_packet_inner` (`src/receiver/mod.rs`) classify verified base packets against the session replay window before assembling responses. `ProcessingContext::replay_verdict` carries the result into Type-12 semantics. `Session::classify_replay` / `Session::commit_replay` (`src/session.rs`) separate classification from mutation and use serial arithmetic across wraparound. State is scoped by both UDP endpoints, SSID, and optional sender micro ID. Tests: `non_monotonic_control_gets_one_u_flagged_reply` (`src/receiver/mod.rs`), session replay tests, and `tests/replay_control_test.rs`. |
| asym-5-08 | "If that value compared to the received in the previous test packet ... is not monotonically increasing, then the Session-Reflector MUST respond with a single reflected packet, setting the U flag to 1 [RFC8972]." | MUST | Reflector | Compliant | `apply_semantic_tlv_processing` (`src/receiver/mod.rs`) sets U on Type 12 and returns no burst behavior for Replay, Reordered, or OutOfWindow verdicts. Requested count, padding, and interval are ignored; both live backends send one normally finalized/signed response. `--drop-replayed` does not suppress handled Type-12 requests. Existing identity, TLV integrity, and address-group checks remain in force. Tests: `non_monotonic_control_gets_one_u_flagged_reply`, `non_monotonic_control_preserves_tlv_validation_and_group_filters`, `non_monotonic_control_does_not_execute_zero_count_or_disabled_requests`, `replay_drop_policy_still_controls_ordinary_packets` (`src/receiver/mod.rs`, including pnet-only builds); `tests/replay_control_test.rs` checks actual reply count/size/U flag, both HMACs, stateless/stateful sequences, wraparound, SSID isolation, and both drop policies over IPv4/IPv6. |
| asym-5-09 | "A Session-Sender SHOULD NOT send the next STAMP test packet with the Reflected Test Packet Control TLV before the Session-Reflector is expected to complete transmitting all reflected packets in response to the ... TLV in the previous test packet." | SHOULD NOT | Sender | Compliant | `Configuration::reflected_burst_pacing_warning` warns when `--send-delay` is shorter than `(count - 1) × interval`; equality is allowed. Configuration tests cover the boundary. This warning does not change pacing or guarantee burst completion (`src/configuration.rs`). |
| asym-5-10 | "When planning In-Service capacity measurement operators SHOULD follow recommendations formulated in Sections 3 and 7 of [RFC7497]." | SHOULD | N-A | N-A | Operator planning guidance pointing to an external document's general recommendations; not a specific, code-testable action. |
| asym-5-11 | "Implementations and operational procedures SHOULD ensure that the use of STAMP for In-Service measurement does not unintentionally degrade data traffic or lead to misinterpretation of ECN-related congestion signals." | SHOULD | N-A | N-A | Aspirational, deployment-dependent goal with no concrete action specified by the draft itself. The closest concrete, testable mechanism (CoS/ECN TLV correctness under RFC 8972 §4.4 / draft-ietf-ippm-stamp-cos-ecn) is out of this draft's own scope and tracked separately. |
| asym-5-12 | "Appropriate thresholds and mitigation actions remain deployment-specific and SHOULD be guided by operator policy and network performance objectives." | SHOULD | N-A | N-A | Pure operator-policy guidance; no protocol action to implement. |
| asym-5-13 | "Section 3.1.5 of [RFC8085] determines that a UDP congestion control SHOULD respond quickly to experienced congestion and account for loss rate and response time when choosing a new rate." | SHOULD | Excluded | Excluded | This is a restatement/citation of RFC 8085's own generic UDP-congestion-control requirement, invoked informatively in this draft's Security Considerations discussion; it is not a new obligation this draft places on Reflected Test Packet Control TLV implementations. General RFC 8085 UDP congestion-control conformance is out of scope for this Type-12-specific matrix. |

## Scope

The matrix excludes keyword boilerplate, editorial/registry instructions, and
requirements imposed on external IP/MPLS networks. L2/L3 Address Group handling
has the sender and test limits stated in its rows. Type 12 is an experimental
draft feature; deployed peers must agree on the profile.
